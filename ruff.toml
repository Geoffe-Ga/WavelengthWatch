line-length = 88
target-version = "py312"
extend-exclude = ["frontend"]

[lint]
# Note: "I" (isort) is excluded - we use standalone isort instead
select = ["E", "F", "W", "B", "UP", "SIM", "C4"]
ignore = ["E203"]
fixable = ["ALL"]
